#include<iostream>
#include "Maze.h"
int main(int argc, char* argv[])
{
	int rows = 5;
	int cols = 5;
	Maze maze = Maze(rows,cols);
	
	
	maze.insert(1);
	vector<int> neighbourIds;
	neighbourIds.

	return 0;
}
